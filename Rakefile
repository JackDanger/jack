# -*- ruby -*-

require 'rubygems'
require 'hoe'
require File.dirname(__FILE__) + '/lib/jack'

Hoe.new('jack', Jack::VERSION) do |p|
  p.developer('Jack Danger Canty', 'jack')
  p.extra_deps = ['johnson', 'rack', 'clip']
  p.extra_dev_deps = ['minitest']
end

# vim: syntax=Ruby
